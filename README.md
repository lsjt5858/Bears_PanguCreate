<div align="center">

# 🐻 Bears PanguCreate

### 企业级测试数据生成平台

[![Python](https://img.shields.io/badge/Python-3.9+-blue.svg)](https://python.org)
[![Flask](https://img.shields.io/badge/Flask-3.0+-green.svg)](https://flask.palletsprojects.com)
[![React](https://img.shields.io/badge/React-18+-61DAFB.svg)](https://reactjs.org)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.0+-3178C6.svg)](https://typescriptlang.org)
[![License](https://img.shields.io/badge/License-Proprietary-red.svg)](./LICENSE)

**快速构造高质量模拟数据，赋能开发测试全流程**

[快速开始](#-快速开始) •
[功能特性](#-核心功能) •
[API 文档](#-api-文档) •
[部署指南](#-部署指南) •
[配置说明](#-配置说明)

</div>

---

## 📖 项目简介

**Bears PanguCreate** 是一款面向企业的专业测试数据生成平台，支持 30+ 数据类型、多表关联生成、定时任务调度、多数据源直连等高级功能。帮助开发团队快速构建真实、合规的测试数据，提升研发效率。

### 为什么选择 PanguCreate？

- 🎯 **开箱即用** - 内置丰富的数据类型和模板，无需复杂配置
- 🔗 **关联生成** - 支持多表关联数据生成，保证数据一致性
- 🔒 **数据合规** - 内置脱敏策略，确保测试数据安全
- ⚡ **高性能** - 单次可生成万级数据，支持批量导出
- 🔌 **易集成** - RESTful API + API Key 认证，轻松对接 CI/CD

---

## ✨ 核心功能

### 数据生成引擎

| 功能 | 描述 |
|------|------|
| **30+ 数据类型** | 中文姓名、手机号、身份证、银行卡、地址、邮箱、UUID 等 |
| **批量生成** | 单次支持 1-10,000 条数据生成 |
| **多格式导出** | JSON、CSV、SQL INSERT 语句 |
| **自定义字段** | 灵活配置字段名称、类型、生成规则 |

### 模板市场

| 功能 | 描述 |
|------|------|
| **预置模板** | 用户注册、电商订单、财务流水等常用场景 |
| **模板共享** | 团队内共享模板，统一数据标准 |
| **评分收藏** | 模板评分、收藏、下载统计 |

### 关联数据生成

| 功能 | 描述 |
|------|------|
| **多表关联** | 支持一对一、一对多、多对多关系 |
| **外键约束** | 自动维护表间引用完整性 |
| **批量导出** | 按依赖顺序导出关联数据 |

### 定时任务

| 功能 | 描述 |
|------|------|
| **Cron 表达式** | 灵活的调度配置 |
| **执行日志** | 完整的任务执行记录 |
| **失败重试** | 自动重试机制 |

### 数据源管理

| 功能 | 描述 |
|------|------|
| **多数据库支持** | MySQL、PostgreSQL、MongoDB |
| **连接测试** | 实时验证数据源连通性 |
| **直接写入** | 生成数据直接写入目标数据库 |

### 企业级特性

| 功能 | 描述 |
|------|------|
| **用户认证** | JWT Token 认证，支持多用户 |
| **API 密钥** | 独立的 API 访问密钥管理 |
| **审计日志** | 完整的操作审计追踪 |
| **数据脱敏** | 多种脱敏策略，保护敏感数据 |
| **Webhook** | 任务完成通知推送 |
| **通知系统** | 站内消息通知 |

---

## 🏗 技术架构


```
┌─────────────────────────────────────────────────────────────┐
│                        Frontend                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   React 18  │  │  TypeScript │  │    Tailwind CSS     │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      Backend API                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   Flask 3   │  │  SQLAlchemy │  │    APScheduler      │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                       Data Layer                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   SQLite    │  │    MySQL    │  │    PostgreSQL       │  │
│  │  (Default)  │  │  (Optional) │  │     (Optional)      │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 技术栈

| 层级 | 技术 |
|------|------|
| **前端** | React 18, TypeScript, Vite, Tailwind CSS, Lucide Icons |
| **后端** | Python 3.9+, Flask 3, SQLAlchemy, APScheduler |
| **数据库** | SQLite (默认), MySQL, PostgreSQL, MongoDB |
| **认证** | JWT (PyJWT), bcrypt |
| **文档** | Swagger/OpenAPI (Flasgger) |

---

## 🚀 快速开始

### 环境要求

- Python 3.9+
- Node.js 18+
- npm 或 yarn

### 安装步骤

```bash
# 1. 克隆项目
git clone https://github.com/your-org/bears-pangucreate.git
cd bears-pangucreate

# 2. 后端设置
cd backend
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt

# 3. 初始化数据库并启动后端
python app.py
# 后端运行在 http://localhost:5001

# 4. 前端设置 (新终端)
cd frontend
npm install
npm run dev
# 前端运行在 http://localhost:5173
```

### 默认账户

首次启动后，可以注册新用户：

```bash
# 注册用户
curl -X POST http://localhost:5001/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "email": "admin@example.com", "password": "admin123"}'
```

---

## 📚 API 文档

启动后端后，访问 Swagger 文档：

```
http://localhost:5001/docs
```

### 主要 API 端点

#### 认证

| 方法 | 端点 | 描述 |
|------|------|------|
| POST | `/api/auth/register` | 用户注册 |
| POST | `/api/auth/login` | 用户登录 |
| GET | `/api/auth/me` | 获取当前用户 |
| POST | `/api/auth/refresh` | 刷新 Token |

#### 数据生成

| 方法 | 端点 | 描述 |
|------|------|------|
| GET | `/api/types` | 获取数据类型列表 |
| POST | `/api/generate` | 生成测试数据 |
| POST | `/api/relation/generate` | 生成关联数据 |

#### 模板管理

| 方法 | 端点 | 描述 |
|------|------|------|
| GET | `/api/templates` | 获取模板列表 |
| POST | `/api/templates` | 创建模板 |
| GET | `/api/market/templates` | 模板市场列表 |
| POST | `/api/market/templates` | 发布模板到市场 |

#### 数据导出

| 方法 | 端点 | 描述 |
|------|------|------|
| POST | `/api/export/json` | 导出 JSON |
| POST | `/api/export/csv` | 导出 CSV |
| POST | `/api/export/sql` | 导出 SQL |

#### 定时任务

| 方法 | 端点 | 描述 |
|------|------|------|
| GET | `/api/scheduled-tasks` | 任务列表 |
| POST | `/api/scheduled-tasks` | 创建任务 |
| POST | `/api/scheduled-tasks/:id/run` | 立即执行 |
| POST | `/api/scheduled-tasks/:id/pause` | 暂停任务 |

#### 数据源

| 方法 | 端点 | 描述 |
|------|------|------|
| GET | `/api/datasources` | 数据源列表 |
| POST | `/api/datasources` | 创建数据源 |
| POST | `/api/datasources/test` | 测试连接 |

---

## 🐳 部署指南

### Docker 部署 (推荐)

```bash
# 构建镜像
docker build -t bears-pangucreate .

# 运行容器
docker run -d \
  -p 5001:5001 \
  -v ./data:/app/backend/data \
  -e SECRET_KEY=your-secret-key \
  bears-pangucreate
```

### Docker Compose

```yaml
version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "5001:5001"
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - DATABASE_URL=sqlite:///data/app.db
    volumes:
      - ./data:/app/data

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
```

### 生产环境配置

```bash
# 使用 Gunicorn 运行
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5001 app:app

# 或使用 uWSGI
pip install uwsgi
uwsgi --http :5001 --wsgi-file app.py --callable app --processes 4
```

---

## ⚙️ 配置说明

### 环境变量

创建 `backend/.env` 文件：

```env
# 应用配置
FLASK_ENV=production
SECRET_KEY=your-super-secret-key-change-in-production

# 数据库配置
DATABASE_URL=sqlite:///data/app.db
# DATABASE_URL=mysql://user:pass@localhost/dbname
# DATABASE_URL=postgresql://user:pass@localhost/dbname

# JWT 配置
JWT_SECRET_KEY=your-jwt-secret-key
JWT_ACCESS_TOKEN_EXPIRES=3600
JWT_REFRESH_TOKEN_EXPIRES=604800

# CORS 配置
CORS_ORIGINS=http://localhost:5173,https://your-domain.com
```

### 配置文件

`backend/config.py` 支持多环境配置：

```python
class ProductionConfig(BaseConfig):
    DEBUG = False
    SQLALCHEMY_DATABASE_URI = os.getenv('DATABASE_URL')
```

---

## 🔒 安全性

### 认证机制

- JWT Token 认证，支持 Access Token + Refresh Token
- API Key 认证，适用于服务间调用
- 密码使用 bcrypt 加密存储

### 数据安全

- 内置数据脱敏功能，支持多种脱敏策略
- 审计日志记录所有敏感操作
- 支持 HTTPS 部署

### 最佳实践

```bash
# 生产环境必须修改
SECRET_KEY=<随机生成的强密钥>
JWT_SECRET_KEY=<随机生成的强密钥>

# 生成随机密钥
python -c "import secrets; print(secrets.token_hex(32))"
```

---

## 📁 项目结构

```
bears-pangucreate/
├── backend/                      # Flask 后端
│   ├── app.py                    # 应用入口
│   ├── config.py                 # 配置文件
│   ├── extensions.py             # Flask 扩展
│   ├── models/                   # 数据模型
│   │   ├── user.py              # 用户模型
│   │   ├── project.py           # 项目模型
│   │   ├── template.py          # 模板模型
│   │   ├── history.py           # 历史记录
│   │   └── ...
│   ├── services/                 # 业务服务
│   │   ├── data_generator_service.py
│   │   ├── template_service.py
│   │   ├── auth_service.py
│   │   └── ...
│   ├── routes/                   # API 路由
│   │   ├── auth_routes.py
│   │   ├── generate_routes.py
│   │   └── ...
│   ├── connectors/               # 数据库连接器
│   │   ├── mysql_connector.py
│   │   ├── postgres_connector.py
│   │   └── mongo_connector.py
│   └── middleware/               # 中间件
│       └── auth.py
│
├── frontend/                     # React 前端
│   ├── src/
│   │   ├── components/          # UI 组件
│   │   ├── pages/               # 页面组件
│   │   ├── context/             # React Context
│   │   ├── hooks/               # 自定义 Hooks
│   │   ├── lib/                 # 工具函数
│   │   └── App.tsx
│   ├── package.json
│   └── vite.config.ts
│
├── README.md
├── LICENSE
└── docker-compose.yml
```

---

## 🤝 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 提交 Pull Request

---

## 📄 许可证

本项目采用专有许可证，源码仅供学习参考。商业使用请联系授权。

详见 [LICENSE](./LICENSE)

---

## 📞 联系我们

- 📧 Email: support@bears.com
- 🐛 Issues: [GitHub Issues](https://github.com/your-org/bears-pangucreate/issues)

---

<div align="center">

**Made with ❤️ by Bears Team**

</div>
